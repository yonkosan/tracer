zerops:
  - setup: api
    build:
      base: nodejs@22
      buildCommands:
        - cd apps/api && npm ci
        - cd apps/api && npm run build
        - cp apps/api/src/db/schema.sql apps/api/dist/db/schema.sql
        - cd apps/api && npm prune --omit=dev
        - cp -r apps/api/dist dist
        - cp -r apps/api/node_modules node_modules
        - cp apps/api/package.json package.json
      deployFiles:
        - dist
        - node_modules
        - package.json
      cache:
        - apps/api/node_modules
    run:
      base: nodejs@22
      ports:
        - port: 3001
          httpSupport: true
      envVariables:
        NODE_ENV: production
        DB_HOST: db
        DB_PORT: ${db_port}
        DB_USER: ${db_user}
        DB_PASSWORD: ${db_password}
        DB_NAME: ${db_dbName}
        REDIS_HOST: cache
        REDIS_PORT: ${cache_port}
        NATS_URL: nats://broker:${broker_port}
        ES_URL: http://search:${search_port}
        ES_USER: elastic
        ES_PASSWORD: ${search_password}
      start: node dist/server.js
      readinessCheck:
        httpGet:
          port: 3001
          path: /health

  - setup: web
    build:
      base: nodejs@22
      buildCommands:
        - cd apps/web && npm ci
        - cd apps/web && npm run build
        - cd apps/web && npm prune --omit=dev
        - cp -r apps/web/.next .next
        - cp -r apps/web/public public
        - cp -r apps/web/node_modules node_modules
        - cp apps/web/package.json package.json
        - cp apps/web/next.config.ts next.config.ts
      deployFiles:
        - .next
        - public
        - node_modules
        - package.json
        - next.config.ts
      cache:
        - apps/web/node_modules
    run:
      base: nodejs@22
      ports:
        - port: 3000
          httpSupport: true
      envVariables:
        NODE_ENV: production
        NEXT_PUBLIC_API_URL: https://api-2c50.prg1.zerops.app
      start: node_modules/.bin/next start -p 3000
      readinessCheck:
        httpGet:
          port: 3000
          path: /
